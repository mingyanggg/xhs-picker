# Hook 机制需求（待启动 · 那哥 6/9 拍板）

> 那哥 2026-06-09 原话："hook 是好东西，我们后期需要用起来"
> **不要现在做**，只存档，等触发时机到了再建

---

## 现状

✅ **L1 弱 Hook（已建）**：`scripts/day1-status.sh` —— 被动查询（你跑 → 我查）
- 用法：`bash scripts/day1-status.sh` 在项目根目录
- 输出：git 提交 / 端口探针 / claude 状态文件 / 关键文件 mtime

## 待启动（那哥拍时机再上）

### L2 中 Hook（半自动轮询）
- 触发：15min 自动跑 day1-status → 写入 log
- 用途：实时知道 claude code 干到哪了
- 风险：cron 噪声（"没变化"也会推）

### L3 强 Hook（文件触发）
- 触发：claude code 改文件 → mtime 变 → 立刻知道
- 需要：fswatch 或 macOS launchd
- 用途：claude code 干完一件事 → 立刻收到通知 → 立刻给那哥分析下一步

## 启动时机（3 个候选）

1. **跨会话频繁**：那哥经常切到 claude code 跑任务时
2. **任务量大**：v3.1 Week 4-5 任务量大时（每天 30+ 文件改动）
3. **每晚复盘**：每天 22:00 cron 复盘时嵌入 day1-status → 那哥每天知道 claude code 今天改了什么

## 嵌入 22:00 复盘方案（最简）

把 `bash scripts/day1-status.sh >> ~/.hermes/logs/xhs-picker-progress.log` 嵌入选品工具 22:00 复盘 cron 的 prompt → 每天自动追加 → 那哥 22:00 复盘报告里直接带进度。

---

**触发条件**：那哥说"现在用 hook" / "上 hook" → 我立刻按上述任一方案建。
