# Goal Declaration — 小红书专用 AI 选品 + 货源对接工具 v3.1

> 🎯 Goal Coding, not Vibe Coding.
> 那哥只写一次 goal，AI 跑完叫那哥验收。
>
> **v1 废**（48 小时硬截止 / 单平台报告）—— 已完成 8/8
> **v2 废**（全平台方法论 / 跨平台 / 5 抓取器）—— 0% 启动
> **v3 废**（小红书单平台 + 货源 + 10 条 must_pass）—— 已被 v3.1 替代
> **v3.1 现行**（小红书单平台 + 货源 + 第三方工具 + 15 条 must_pass）—— 2026-06-09 立项

---

## 1. goal（一句话终点）

**为那哥自己做一个本地化的"小红书选品 + 货源反查 + 第三方工具集成 + 极致安全"桌面工具**：
- 用户输入关键词 → 工具在小红书做选品分析（市场/爆款/蓝海）
- 选中候选品 → 工具自动反查到 1688/拼多多/淘宝 找同款/相似款 + 利润空间
- 集成千瓜/灰豚/蝉妈妈/巨量算数/抖音精选联盟/小红书APP 6 个第三方数据源（**Tauri 内置浏览器 + CDP 协议**）
- AI 算法从 11 篇选品手册里筛选可靠的货源平台 + 给商家做双轨可信度评分
- 4 层级极致安全（账号/IP/数据/降级），账号不被封、IP 不被风控、数据可验证、商家不踩坑
- **只做小红书**（不碰抖音/快手/视频号作为选品目标）

**v3.1 相对 v3 的核心变化**（那哥 6/9 决策）：

| 维度 | v3 | v3.1 |
|---|---|---|
| must_pass | 10 条 | **15 条**（+5）|
| 第三方工具 | 无 | **6 个集成**（千瓜/灰豚/蝉妈妈/巨量算数/抖音精选联盟/小红书APP）|
| 选品方向 | 笼统 | **7 大方向**（功能性半标品/红海蓝海/美妆/自带流量/大牌平替/新奇特/知识付费）|
| 运营节奏 | 无 | **漏斗池 + 测品**（每天3+新品+15天数据周期）|
| 商家评分 | 单轨 | **双轨**（小红书商家 + 货源商家 分别打分）|
| 中高端原则 | 无 | **AI 选品必须提示中高端定位**|
| 痛点判断 | 无 | **痛点 vs 痒点必须判定**（影响预算）|
| 跟品方法 | 无 | **自动识别爆品 → 反查货源 → 双策略**|
| 传统电商验证 | 无 | **淘宝/京东数据交叉验证**|
| 总工时 | 60-80h | **80-100h**（+25%）|
| 完成周期 | 3-4 周 | **5 周**（20-25 工作日）|

---

## 2. acceptance（验收标准）

### 2.1 must_pass（15 条，少一条不算完成）

```yaml
must_pass:
  # ===== v3 原有 10 条 =====
  - id: M1
    name: "小红书选品功能可用"
    verify: |
      输入品类关键词（如'防晒霜'），工具自动抓取小红书笔记列表
      输出结构化选品分析（市场概况/爆款潜力/蓝海评分/选品建议）
      注：不抓抖音/快手/视频号，砍掉

  - id: M2
    name: "货源反查功能（含跟品）"
    verify: |
      选中一个候选品 → 工具自动反查 1688/拼多多/淘宝
      输出：同款/相似款列表 + 售价 + 起订量 + 利润空间
      **含跟品逻辑**：自动识别小红书爆品 → 反查货源 → 输出"跟品+超越"双策略
      至少 3 个货源平台全部跑通 = 通过

  - id: M3
    name: "AI 算法筛选货源（基于 11 篇手册）"
    verify: |
      AI 算法必须基于 11 篇手册的至少 5 个方法做筛选：
      - G1 蓝海三部曲（稳定流量+产品很差+稳定变现）
      - G2 六步选品法
      - G4 痛点 vs 痒点（痛点能确定预算，痒点不能）
      - G5 跟品方法
      - X2 低粉爆款法
      - X8 漏斗池
      - X9 选品方向
      **必须引用原文做依据**（脱敏后）
      **必须包含痛点判定维度**（G4）

  - id: M4
    name: "极致安全反爬（账号/IP/数据/降级）"
    verify: |
      账号层：Cookie 加密存储（SQLite） + 自动检测风控 + 不共用账号
      IP 层：请求频率自适应（≤人类操作）+ 支持用户自配代理池
      数据层：抓取后做假数据过滤 + 数据源标注 + 校验逻辑
      降级层：CDP 失败 → 手动粘贴模式（绝不反复重试）
      4 层级全部实现 = 通过

  - id: M5
    name: "数据准确性可验证（含交叉验证）"
    verify: |
      每个数据字段有数据源标注（来自哪个笔记/哪个商家）
      用户可点击回查原始页面
      AI 必须标注"置信度"（高/中/低）+ 数据采集时间
      **支持传统电商平台数据交叉验证**（淘宝/京东官方数据对照）

  - id: M6
    name: "货源商业可信度评分（双轨）"
    verify: |
      双轨评分（必须分别独立打分）：
      - 小红书商家 0-100 分（基于 X4 灰豚商品榜数据）
      - 货源商家 0-100 分（基于 1688/拼多多/淘宝 数据）
      评分因子 = 销量 + 评价 + 经营时长 + AI 风险提示
      低分商家标红 + 给出"建议放弃"理由

  - id: M7
    name: "黑五类只警告不推广（不提供货源）"
    verify: |
      输入黑五类关键词（减肥药/壮阳/医美等）→ 弹出红色警告
      不提供黑五类货源搜索（不返回任何结果）
      不引导黑五类引流方式

  - id: M8
    name: "小红书单平台极致优化"
    verify: |
      砍掉所有非小红书平台的选品抓取
      抖音/快手/视频号仅作为"反查已选品在其他平台表现"（v3.1+）
      主流程不出现跨平台选品入口

  - id: M9
    name: "本地存储不联网"
    verify: |
      所有用户数据本地 SQLite 存储
      工具不向任何外部服务器上报用户数据
      DeepSeek API 仅用于分析，不存储 prompt 内容
      网络请求白名单：仅小红书 + 1688/拼多多/淘宝 + 6 个第三方工具 + DeepSeek

  - id: M10
    name: "数据不被污染"
    verify: |
      抓取时识别假数据/刷量数据：
      - 单笔记 24h 突然爆 10x 互动 → 标记"疑似刷量"
      - 商家销量异常波动 → 标记"数据异常"
      AI 综合判断后给出"数据可信度"评分

  # ===== v3.1 新增 5 条 =====
  - id: M11
    name: "选品漏斗池"
    verify: |
      跟踪模块的核心业务规则（来自 04-小红书 X8）：
      - 每天 3+ 新品入库
      - 每个品分配 3 条笔记（ABC 测试）
      - 出单的品 → 自动盯相似款（反查其他平台同款）
      - 上品日历 UI（早 8/9:30/11:30/14/16/18/20/21/22 共 9 时点提醒）

  - id: M12
    name: "选品方向支持 7 大方向"
    verify: |
      工具必须支持以下 7 大选品方向（来自 04-小红书 X9）：
      1. 功能性半标品（如瑜伽服、防晒服）
      2. 红海市场中的蓝海（如大胸内衣、小个子女鞋）
      3. 美妆护肤品
      4. 自带流量的产品（如情侣内衣、JK 服、汉服）
      5. 大牌平替
      6. 新奇特 + 真实需求
      7. 知识付费类

  - id: M13
    name: "测品功能"
    verify: |
      每个品测品规则（来自 04-小红书 X10）：
      - 测 3 个不同封面 + 3 个不同标题
      - 15 天数据周期
      - 7 天 0 销自动标红 + 建议放弃
      - 无销量品自动下架

  - id: M14
    name: "中高端路线原则"
    verify: |
      AI 选品建议必须遵循（来自 04-小红书 X11）：
      - 提示"中高端定位"
      - 给出"高价卖点挖掘"建议
      - 拒绝低价走量品

  - id: M15
    name: "第三方数据工具集成（5+1）"
    verify: |
      集成 5 个主第三方数据源（来自 04-小红书 X3-X5）：
      1. 千瓜 100 品类（小红书）
      2. 灰豚数据商品榜（小红书）
      3. 蝉妈妈视频商品榜（抖音→小红书选品）
      4. 巨量算数（趋势分析）— **免费，直接抓**
      5. 小红书 APP 搜索 — **免费，直接抓**

      **1 个可选数据源**：
      6. 抖音精选联盟（参考性数据）— 仅当用户有商品橱窗时启用，否则跳过

      **登录方式（那哥 6/9 决策：闲鱼共享账号方案）**：
      - 千瓜 / 灰豚数据 / 蝉妈妈 / 抖音精选联盟 = **闲鱼购买共享账号**
      - 巨量算数 = **免费，直接抓**
      - 小红书 APP 搜索 = **免费，直接抓**
      - 工具集成 = Tauri 内置 webview + CDP 9223 协议
      - Cookie 加密存 SQLite（账号层）
      - 失败降级为手动模式（用户自助）

      **⚠️ 闲鱼共享账号风险警告弹窗（必须实现）**：
      工具必须在用户首次添加"千瓜/灰豚/蝉妈妈/抖音精选联盟"任一数据源时，
      弹出风险警告对话框，强制用户勾选"已知风险"才能继续：

      > ⚠️ 闲鱼共享账号方案风险提示
      >
      > 1. 账号倒卖违反千瓜/灰豚/蝉妈妈/抖音精选联盟用户协议，存在法律风险
      > 2. 闲鱼卖方账号可能任意时刻被踢下线（卖方改密/卖给别人/被官方封）
      > 3. 数据可信度受卖方账号状态影响（账号过期后数据可能失真）
      > 4. 你的工具不为账号被封 / 数据丢失 / 任何法律后果负责
      >
      > 决策溯源：那哥 2026-06-09 拍板坚持采用此方案（小米曾 3 次明确反对）
      >
      > ☐ 我已知上述风险，自行承担责任

      **安全约束**：
      - 不存储用户账号密码
      - Cookie 加密存 SQLite
      - 工具不直接提供账号获取渠道（用户在闲鱼自行购买）
      - 用户对自己账号的合法性负责
      - 工具必须保留"删除闲鱼账号 / 切换其他方案"的入口

      **决策溯源（必须保留在 GOAL）**：
      - 6/9 那哥决策：闲鱼方案
      - 6/9 小米立场：3 次明确反对（违规/失效/可信度 3 大风险）
      - 6/9 最终：那哥拍板坚持
      - 7/9 复盘：若出问题，从这里查决策原因

      **数据可信度**（M5 兜底）：
      - 每个数据字段有来源标注
      - 假数据检测（M10）兜底
      - 跨平台交叉验证
      - 闲鱼账号失效时自动提示重新登录

  - id: M16
    name: "多模态分析（封面+视频）"
    verify: |
      **分析对象**（那哥 2026-06-09 决策）：
      - 小红书笔记封面图（爆品潜力分析）
      - 1688/拼多多/淘宝 商家商品主图（货源质量判断）
      - 小红书爆款视频（视频拆解，中长期）
      **触发场景**：
      - M1 选品分析报告里，AI 必须能"看"候选品相关图片
      - M6 商家评分里，AI 必须能"看"商家商品主图（识别 P 图/盗图/低质图）
      - M13 测品里，AI 必须能分析封面图风格（辅助测品）
      **AI 模型选择**（多模态）：
      - 首选：智谱 GLM-4V/GLM-4V-Plus（**国内最稳**）
      - 备选：DeepSeek-VL2（**需先验证 API 稳定性**）
      - 不推荐：MiniMax 多模态（生产环境不稳定）
      - 文本任务继续用 DeepSeek
      **安全约束**：
      - 图片传输走 HTTPS
      - 不存储用户上传的图片
      - API key 不中转不存储
      **降级**：多模态 API 失败 → 纯文本分析 + 提示用户
      **工时**：+10-15h（多模态接口封装 + 提示词工程 + 降级逻辑）

  - id: M17
    name: "渐进式登录流程（UX 设计）"
    verify: |
      **那哥 2026-06-09 决策**：
      - Q1 登录流程 → A. 方案 1（**首次启动引导**）
      - Q2 是否强制 → A. **强制登录**（至少 1 个数据源）

      **3 阶段设计**：

      **阶段 1：首次启动引导（30 秒搞定）**
      - 弹出"快速开始"对话框
      - 必选 1 个数据源：推荐组合"巨量算数（免费）+ 小红书 APP 搜索（免费）"
      - 可选 0-3 个货源平台（1688 / 拼多多 / 淘宝）
      - 老用户可在设置里关闭此引导（首次跳过按钮必须保留）
      - **强制约束**：必须至少选 1 个数据源才能进入主界面

      **阶段 2：用工具时按需登录**
      - 用户点击 M15 第三方数据功能（如"用千瓜查品类"）时
      - 弹出"添加数据源"对话框（不是 6 个全弹，只弹未登录的）
      - **M15 闲鱼共享账号必须弹风险警告**（强制勾选"已知风险"）
      - 用户选"现在登录"或"暂时跳过"

      **阶段 3：登录后持久化**
      - Cookie 加密存 SQLite（已有规划，账号层）
      - 7 天 / 30 天 / 永久（按平台配置）
      - 失效前 3 天提醒"重新登录"

      **配套 UX**：
      - 设置页有"数据源管理"入口（随时加 / 删 / 重新登录 / 删除闲鱼账号 / 切换方案）
      - 主界面顶部"数据源状态指示灯"：
        - 🟢 绿灯 = 已登录且有效
        - 🟡 黄灯 = 试用中 / 即将失效
        - 🔴 红灯 = 未登录
      - 首次启动引导可手动跳过（不强制重看）

      **决策溯源**：
      - 6/9 那哥 A+A 决策：首次启动引导 + 强制至少 1 个
      - 备选 B（懒登录）+ A（强制）被否
      - 备选 C（混合）+ 不强制 被否

      **工时**：+4-6h
      - 首次启动引导 UI = 2h
      - 数据源管理页 = 1.5h
      - 状态指示灯组件 = 0.5h
      - 风险警告弹窗（与 M15 共享）= 复用

  - id: M13  # v3.1 升级版（原 M13 + 飞书文章 3 大核心）
    name: "测品功能 v2（飞书文章升级版）"
    verify: |
      **品类判断**（新增 · 来自飞书文章 3.1+3.2）：
      - 工具必须先判断候选品是【有颜】还是【有用】
      - 【有颜】= 饰品/服装/手机壳/鞋子/宝宝用品 → 重点放**选款式**，5 条左右测试
      - 【有用】= 功效品/工具/服务 → 卖点重组，**多测试几条**
      - 已爆起来的【有用】：直接在原爆款标题改几个字
      - 未爆起来的【有用】：卖点重组 + 多测

      **封面二阶段**（新增 · 来自飞书文章 2.1）：
      - 第一阶段：小红书直接搜产品名 → 按"**最热**"排序 → 找 2-3 张同风格爆款图
      - 进阶阶段：1:1 模仿爆款图（文字+颜色+布局全部复用）
      - **必须配合 M16 多模态**（AI 分析封面风格特征）

      **卖点重组 4 步 SOP**（新增 · 来自飞书文章 2.3.2）：
      1. 拆解爆款标题的卖点
      2. 找出核心属性（不能换的部分）
      3. 修饰部分替换（形容词/场景描述换新）
      4. 重组为新标题

      **视频剪辑 SOP**（来自飞书文章 2.2）：
      - 3 秒内换素材
      - 不改音频
      - 前 3 秒保留爆款素材
      - **不做 1:1 完全搬运**（合规约束：那哥 2026-06-09 决策）

      **原有 15 天数据周期 + 7 天 0 销自动标红 + 无销量自动下架**（沿用 v3.1 M13）

      **工时增加**：+4-6h（飞书文章方法论注入 + SOP 实现）
```

### 2.2 nice_to_have（5 条，v3.1+ 考虑）

```yaml
nice_to_have:
  - name: "周期性选品日历（来自 02-通用 G6）"
    verify: "按月/节日预判应季品，提前 1 个月提醒"
  - name: "圈层选品（来自 11-其他 O1）"
    verify: "按职业/地域/文化圈层定位人群 + 选品"
  - name: "AI 反向评估（M7 灵魂 · 来自 02-通用 + 04-小红书）"
    verify: "AI 出 3 候选 + AI 砍 1 + 用户砍 1 + 入选 1 + 风险清单"
  - name: "对标账号跟踪（小红书单平台）"
    verify: "添加对标账号后定期抓取其小红书数据变化"
  - name: "多平台反查（已选品在抖音/视频号表现）"
    verify: "v3.2+ 启用，不做新选品，只看已选品跨平台表现"
```

---

## 3. budget（预算边界）

```yaml
budget:
  max_hours: null            # 不设硬截止
  max_steps: null            # 不设步骤上限
  max_iterations: 10         # 每个 must_pass 最多自动迭代 10 次
  stop_conditions:
    - "所有 15 条 must_pass 通过 → 完成，通知那哥验收"
    - "同一 must_pass 连续 10 次失败 → 暂停 + 通知那哥"
    - "触及黑五类推广场景 → 立即停止 + 通知那哥"
    - "账号被封/IP 被风控 → 立即降级为手动模式 + 通知那哥"
    - "触及任何外部付费操作 → 暂停 + 等那哥确认"

  time_estimate:
    total_hours: 80-100
    working_days_at_4h: 20-25
    calendar_weeks: 5
```

---

## 4. deliverables（交付物）

```
~/AI项目/xhs-picker/
├── README.md                  # 项目说明（v3.1 定位）
├── CLAUDE.md                  # Claude Code 配置（v3.1 已更新）
├── GOAL-DECLARATION.md        # 本文件（v3.1 现行）
├── GOAL-DECLARATION.v1.bak    # v1 备份
├── GOAL-DECLARATION.v2.bak    # v2 备份
├── GOAL-DECLARATION.v3.bak    # v3 备份（被 v3.1 替代）
├── 方法论学习笔记.md           # 选品方法论（v3.1 重构为小红书专用）
├── PROGRESS.md                # v1 进度
├── PROGRESS-v3.1.md           # v3.1 进度
│
├── src-tauri/                 # Tauri 2 桌面端壳（沿用 v1）
├── src/
│   ├── app/                   # 界面
│   │   ├── page.tsx           # 主界面（小红书选品 + 货源反查 + 第三方工具入口）
│   │   ├── tracker/           # 跟踪模块（M11 漏斗池）
│   │   └── components/
│   │       ├── SearchBox.tsx          # 关键词输入
│   │       ├── ReportCard.tsx         # 选品报告（4 板块）
│   │       ├── SourceCompare.tsx      # 货源对比表（1688/拼多多/淘宝）
│   │       ├── ProfitCalculator.tsx   # 利润空间计算
│   │       ├── ReliabilityBadge.tsx   # 双轨可信度评分（M6）
│   │       ├── BlacklistAlert.tsx     # 黑五类警告
│   │       ├── BrowserPanel.tsx       # 内置浏览器（M15 第三方工具登录）
│   │       ├── SafetyIndicator.tsx    # 极致安全状态指示
│   │       ├── FunnelPool.tsx         # 【v3.1 新增】M11 漏斗池
│   │       ├── TestProduct.tsx        # 【v3.1 新增】M13 测品
│   │       └── CategorySelector.tsx   # 【v3.1 新增】M12 7 大方向
│   ├── lib/
│   │   ├── platforms/             # 平台层
│   │   │   ├── types.ts           # Platform 接口
│   │   │   ├── xhs.ts             # 小红书专用抓取器
│   │   │   ├── source-1688.ts     # 1688 货源抓取
│   │   │   ├── source-pdd.ts      # 拼多多货源抓取
│   │   │   ├── source-taobao.ts   # 淘宝货源抓取
│   │   │   ├── thirdparty/        # 【v3.1 新增】M15 第三方工具
│   │   │   │   ├── qiangua.ts     # 千瓜
│   │   │   │   ├── huitun.ts      # 灰豚
│   │   │   │   ├── chanmama.ts    # 蝉妈妈
│   │   │   │   ├── juliang.ts     # 巨量算数
│   │   │   │   ├── douyin-jingxuan.ts # 抖音精选联盟
│   │   │   │   └── xhs-app.ts     # 小红书 APP 搜索
│   │   │   └── generic.ts         # 通用兜底
│   │   ├── methods/               # AI 算法（v3.1 5 个 + 1 个痛点）
│   │   │   ├── source-matcher.ts  # 货源平台匹配
│   │   │   ├── profit-calculator.ts # 利润空间
│   │   │   ├── reliability-scorer.ts # 双轨可信度评分
│   │   │   ├── fake-data-detector.ts # 假数据检测
│   │   │   ├── pain-point-judge.ts # 【v3.1 新增】G4 痛点判定
│   │   │   ├── follow-product.ts  # 【v3.1 新增】G5 跟品方法
│   │   │   └── index.ts
│   │   ├── safety/                # 极致安全 4 层级
│   │   │   ├── account.ts         # 账号层
│   │   │   ├── network.ts         # IP 层
│   │   │   ├── data-integrity.ts  # 数据层
│   │   │   └── fallback.ts        # 降级层
│   │   ├── analyzer.ts            # AI 选品分析（小红书专用 + M14 中高端原则）
│   │   ├── blacklist.ts           # 黑五类关键词库
│   │   ├── categories.ts          # 9 大品类 + M12 7 大方向
│   │   └── tracker/               # 跟踪模块（M11 漏斗池）
│   │       ├── store.ts
│   │       ├── scheduler.ts       # 上品日历 9 时点
│   │       ├── funnel.ts          # 【v3.1 新增】漏斗池
│   │       └── lifecycle.ts
│   └── api/
│       ├── analyze/               # 选品分析 API
│       ├── source/                # 货源反查 API
│       └── thirdparty/            # 【v3.1 新增】第三方工具 API
│
├── 学习笔记/                    # 每天 1-2h 学习产出
│
├── tests/                        # 自检 + 单元测试
└── .goal-state/                  # Goal Coding 状态存档
```

---

## 5. constraints（约束条件）

```yaml
constraints:
  must:
    - "技术栈：Tauri 2 + Next.js + TypeScript + Tailwind CSS（沿用 v1）"
    - "AI 调用：开发期 DeepSeek；运行时支持用户自带 API Key（智谱/MiniMax/DeepSeek 任选）"
    - "平台层：platforms/ 统一 Platform 接口（沿用 v1 设计）"
    - "极致安全 4 层级全部实现（账号/IP/数据/降级）"
    - "**v3.1 第三方工具登录方式**：Tauri 内置 webview + CDP 9223 协议（不是系统 Chrome）"
    - "**v3.1 双轨商家评分**：小红书商家 + 货源商家 分别独立打分"
    - "**v3.1 痛点判定**：AI 选品必须先判定"痛点 vs 痒点"（影响预算和转化）"
    - "**v3.1 中高端原则**：AI 选品建议必须拒绝低价走量品"
    - "**v3.1 漏斗池**：每天 3+ 新品入库 + 9 时点上品日历"
    - "本地存储：SQLite（沿用 v1）"
    - "学习产出：每天 1-2h 学习必须写到 学习笔记/ 目录"
    - "黑五类不推广、不提供货源、不引导引流方式"
    - "数据源标注：每个数据字段必须有来源"
    - "降级优先：CDP 失败时立即降级为手动粘贴，不反复重试"

  must_not:
    - "不引入付费 SaaS 依赖"
    - "不存储用户隐私数据"
    - "不碰黑五类产品推广"
    - "不中转/不存储/不代理用户的 API Key"
    - "不抓取抖音/快手/视频号作为选品目标"
    - "不向外部服务器上报用户数据"
    - "不返回假数据/刷量数据未标记的选品报告"
    - "**v3.1 新增**：第三方工具登录不使用系统 Chrome（避免账号混合风险）"
```

---

## 6. context（上下文）

```yaml
context:
  primary_user: "那哥自己"
  secondary_user: "OPC 创业者 + 小微企业"

  # 必须吸收的 11 篇手册方法（v3.1 完整对齐）
  required_methods:
    G1_蓝海三部曲: "稳定流量+产品很差+稳定变现"
    G2_六步法: "找对标→拆节点→匹配能力→1:1模仿→做最佳→超越"
    G4_痛点痒点: "痛点能确定预算，痒点不能"
    G5_跟品: "爆品截流 → 反查货源"
    X2_低粉爆款: "<200粉+7天100赞+销量>100"
    X4_灰豚: "商品分析-带货商品榜"
    X5_蝉妈妈: "视频商品榜"
    X8_漏斗池: "每天3+新品+ABC测试+相似款跟踪"
    X9_7大方向: "功能性半标品/红海蓝海/美妆/自带流量/大牌平替/新奇特/知识付费"
    X10_测品: "3封面+3标题+15天数据周期"
    X11_中高端: "小红书要敢于开高价"

  # 11 篇手册路径
  manuals_path: "~/Documents/Obsidian Vault/iCloud/_Base/AI变现/知识库/选品手册/"
  # 真实路径
  manuals_actual: "/Users/michael/Library/Mobile Documents/iCloud~md~obsidian/Documents/iCloud/_Base/AI变现/知识库/选品手册/"

  # 第三方工具登录方式（v3.1 关键决策）
  thirdparty_login:
    method: "Tauri 内置 webview + CDP 9223 协议"
    storage: "Cookie 加密存 SQLite（账号层）"
    fallback: "CDP 失败 → 提示用户手动复制粘贴"
    why: "避免系统 Chrome 账号混合风险 + 隔离风控"

  # 极致安全 4 层级
  safety_4_layers:
    account: "Cookie 加密 + 风控检测 + 不共用账号"
    network: "请求频率自适应 + 代理池支持"
    data: "假数据过滤 + 数据源标注 + 校验逻辑"
    fallback: "CDP 失败立即降级手动模式"

  # 黑五类处理
  blacklist_handling:
    v1: "只警告不推广"
    v3: "不提供黑五类货源搜索 + 不引导引流方式"
    v3.1: "同 v3"

  # 学习内容
  learning_sources:
    - "生财有术 AI 编程深海圈课程"
    - "X / Reddit 上的新 AI 技术"
    - "其他 AI 相关课程"
  learning_output: "必须写笔记到 xhs-picker/学习笔记/ 目录"

  # v1/v2/v3 遗产
  v1_completed: "8 条 must_pass 全部 ✅"
  v2_abandoned_reason: "5 平台全抓复杂度太高"
  v3_replaced_reason: "11 篇手册对齐后必须升级到 15 条 must_pass"
```

---

## 7. AI 自检 + 迭代机制

```yaml
self_check:
  step_1_syntax: "npx tsc --noEmit → 0 errors"
  step_2_build: "pnpm build --no-lint → Build success"
  step_3_unit_test: "pnpm test src/lib/methods/ → All tests passed"
  step_4_runtime: "scripts/runtime-check.sh → 0 崩溃 / 0 白屏"
  step_5_method_invoke: "6 个 AI 算法独立调用 → 全部输出符合 spec"
  step_6_safety_verify: "极致安全 4 层级全部跑通"
  step_7_e2e: "5 次完整选品+货源反查+第三方工具流程 → 5/5 成功"

iteration:
  max_iterations: 10
  log_path: ".goal-state/iterations/<must_pass_id>_<n>.log"
  failure_pattern_detection:
    - "同一错误连续 3 次 → 暂停 + 通知那哥"
    - "黑五类检测失效 → 立即暂停 + 通知那哥"
    - "账号被封/IP 被风控 → 立即降级 + 通知那哥"
```

---

## 8. checkpoints（中间检查点，10 个 C0-C9）

```yaml
checkpoints:
  - id: C0
    after: "v3.1 GOAL 写入 + v1/v2/v3 备份 + PROGRESS-v3.1.md 初始化"
    notify: "✅ Checkpoint 0: v3.1 GOAL 已就位"

  - id: C1
    after: "砍掉 kuaishou/douyin/shipinhao + 新增 3 个货源抓取器骨架"
    notify: "✅ Checkpoint 1: 平台层收敛完成"

  - id: C2
    after: "极致安全 4 层级基础实现"
    notify: "✅ Checkpoint 2: 极致安全 4 层级就位"

  - id: C3
    after: "M2 货源反查端到端跑通（1688 优先）"
    notify: "✅ Checkpoint 3: 货源反查可用"

  - id: C4
    after: "M3 AI 算法筛选货源（含痛点判定）+ M6 双轨商家评分"
    notify: "✅ Checkpoint 4: AI 货源算法可用"

  - id: C5
    after: "M5 数据准确性 + M10 假数据检测 + M14 中高端原则"
    notify: "✅ Checkpoint 5: 数据可信度可用"

  - id: C6
    after: "M15 第三方工具集成（6 个全部跑通）"
    notify: "✅ Checkpoint 6: 第三方工具集成完成"

  - id: C7
    after: "M11 漏斗池 + M12 7 大方向 + M13 测品"
    notify: "✅ Checkpoint 7: 运营功能就位"

  - id: C8
    after: "7 步自检全过 + E2E 5/5 成功"
    notify: "✅ Checkpoint 8: 自检全过 + 等那哥验收"

  - id: C9
    after: "那哥验收通过"
    notify: "🎉 v3.1 交付完成 + 启动 v3.2 计划（nice_to_have）"
```

---

## 9. escalation（升级机制，12 个场景）

```yaml
escalation:
  # v1 保留
  - trigger: "小红书账号被封"
    action: "立即停止所有抓取 + 通知那哥 + 等那哥处理账号"
  - trigger: "IP 被风控/限流"
    action: "降级为手动粘贴模式 + 建议配置代理池"
  - trigger: "Tauri 编译环境搭建失败"
    action: "降级为 Electron + 通知那哥"
  - trigger: "任意平台反爬检测导致数据无法读取"
    action: "降级为手动粘贴模式 + 记录反爬策略"
  - trigger: "触及任何外部付费操作"
    action: "暂停 + 等那哥确认"
  - trigger: "DeepSeek API 限速 429"
    action: "自动降级 glm-4.7-flash"
  - trigger: "v1 8 条 must_pass 回归失败"
    action: "立即修复（v3.1 不能破坏 v1）"

  # v3.1 新增
  - trigger: "1688/拼多多/淘宝账号被封"
    action: "立即停止该平台抓取 + 通知那哥 + 等处理"
  - trigger: "货源商家被举报为钓鱼/虚假"
    action: "自动标红 + 加入黑名单 + 通知那哥"
  - trigger: "黑五类检测失效（漏报）"
    action: "立即暂停 + 通知那哥 + 检查 prompt 模板"
  - trigger: "数据源标注缺失（不可回查）"
    action: "该数据不可用 + 报告缺失率 + 等那哥修复"
  - trigger: "第三方工具（千瓜/灰豚/蝉妈妈）登录失败"
    action: "降级为工具跳转链接（用户自行操作）+ 通知那哥"
```

---

## 10. v3.1 升级变更摘要

| 维度 | v1 | v2 | v3 | v3.1 |
|---|---|---|---|---|
| 目标平台 | 小红书 | 5 平台 | 小红书 | **小红书** |
| must_pass | 8 条 | 18 条 | 10 条 | **15 条** |
| 第三方工具 | 无 | 无 | 无 | **6 个**（千瓜/灰豚/蝉妈妈/巨量算数/抖音精选联盟/小红书APP）|
| 选品方向 | 笼统 | 跨平台 | 笼统 | **7 大方向** |
| 运营节奏 | 无 | 跨平台漏斗 | 无 | **漏斗池+测品** |
| 商家评分 | 无 | 无 | 单轨 | **双轨** |
| 中高端原则 | 无 | 无 | 无 | **必含** |
| 痛点判定 | 无 | 无 | 无 | **必含** |
| 跟品方法 | 无 | 无 | 无 | **必含** |
| 传统电商验证 | 无 | 无 | 无 | **必含** |
| 真实工时 | 48h | 60-90h | 60-80h | **80-100h** |
| 完成周期 | 2 天 | 4-6 周 | 3-4 周 | **5 周** |

---

## 11. 质量自检清单

- [x] **goal**：小红书选品 + 货源反查 + 第三方工具 + 极致安全
- [x] **acceptance**：15 条 must_pass 全部可验证
- [x] **budget**：不设硬截止 + 80-100h 真实预期 + 5 周
- [x] **deliverables**：Tauri 2 + 6 个第三方工具 + 双轨评分 + 漏斗池
- [x] **constraints**：明确不做什么 + 黑五类不碰 + 第三方登录用内置 webview
- [x] **context**：11 篇手册的 11 个方法全部纳入 + 第三方登录方式
- [x] **escalation**：12 个升级场景（含第三方登录失败降级）
- [x] **AI 自检**：7 步自检 + 失败自动迭代 + 阻塞通知
- [x] **checkpoints**：10 个（C0-C9）
- [x] **v3.1 边界纪律**：本工具是选品+货源+第三方集成工具，不混入 B2B 交付能力
- [x] **v3.1 反 AI 痕迹**：所有目标可量化（15 must_pass / 6 第三方 / 4 安全层 / 10 checkpoints）

---

## 12. 执行步骤预览（v3.1，**不设步骤上限**）

```
Step 0:  v1/v2/v3 备份 + v3.1 GOAL 写入 + PROGRESS-v3.1.md 初始化
Step 1:  砍掉 kuaishou/douyin/shipinhao + 新增 3 个货源抓取器骨架
Step 2:  极致安全 4 层级基础实现
Step 3:  M2 货源反查端到端（1688 优先，含 G5 跟品）
Step 4:  M3 AI 算法筛选货源（含 G4 痛点判定）+ M6 双轨商家评分
Step 5:  M5 数据准确性 + M10 假数据检测 + M14 中高端原则
Step 6:  M15 第三方工具集成（6 个，Tauri 内置 webview + CDP）
Step 7:  M11 漏斗池 + M12 7 大方向 + M13 测品
Step 8:  v1 8 条 must_pass 回归测试
Step 9:  7 步自检全过 + E2E 5/5 成功
Step 10: 通知那哥验收
```

**v3.1 灵魂一句话**：**把小红书选品 + 6 个第三方数据源 + 1688/拼多多/淘宝货源反查，全部集成到一个极致安全的本地工具里，让那哥自己先用上。**

---

**当前状态**：🟢 v3.1 立项完成（15 条 must_pass）
**最后更新**：2026-06-09
**当前 owner**：那哥
